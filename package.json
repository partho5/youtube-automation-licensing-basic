{
  "name": "license-api-vercel",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "echo \"No build step required\"",
    //"dev": "vercel dev"
  }
}